backupCleanup:
  schedule: "0 23 * * *"
  retainPerVolume: 7

csi:
  attacherReplicaCount: 3
  provisionerReplicaCount: 3
  resizerReplicaCount: 3
  snapshotterReplicaCount: 3

defaultBackupStore:
  backupTarget: s3://longhorn-backup@eu-east-local/
  backupTargetCredentialSecret: minio

defaultSettings:
  autoCleanupSnapshotAfterOnDemandBackupCompleted: true
  autoCleanupSnapshotWhenDeleteBackup: true
  autoCleanupSystemGeneratedSnapshot: true
  concurrentAutomaticEngineUpgradePerNodeLimit: "1"
  defaultDataPath: "/ext/persistent"
  defaultReplicaCount: "2"
  orphanAutoDeletion: true
  replicaReplenishmentWaitInterval: "0"
  removeSnapshotsDuringFilesystemTrim: true
  storageReservedPercentageForDefaultDisk: "1"
  snapshotMaxCount: "8"

# ingress:
#   enabled: false
#   ingressClassName: cilium
#   host: ''
#   tls: true
#   tlsSecret: longhorn-tls
#   pathType: Prefix
#   annotations:
#     cert-manager.io/cluster-issuer: cloudflare-letsencrypt-production
#     ingress.cilium.io/loadbalancer-mode: shared

longhornManager:
  # keep at least 2 of 3 managers available during rollouts instead of the chart
  # default of restarting all of them at once
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 1

longhornUI:
  replicas: 2

# metrics:
#   serviceMonitor:
#     enabled: true

persistence:
  defaultClassReplicaCount: 2
  migratable: false
  reclaimPolicy: Retain
  unmapMarkSnapChainRemoved: enabled

preUpgradeChecker:
  jobEnabled: false

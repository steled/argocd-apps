image:
  tag: ''

myjd:
  existingSecretName: myjd

persistence:
  accessMode: ReadWriteMany
  enabled: true
  size: 100Gi
  storageClass: longhorn

service:
  myjd:
    enabled: true

  web:
    enabled: false

# strategy:
#   type: Recreate

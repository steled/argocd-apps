# ingress:
#   enabled: false
#   className: cilium
#   annotations:
#     cert-manager.io/cluster-issuer: cloudflare-letsencrypt-production
#     ingress.cilium.io/loadbalancer-mode: shared
#   hosts:
#     - host: ''
#       paths:
#         - path: /
#           pathType: Prefix
#   tls:
#     - secretName: jellyfin-tls
#       hosts:
#         - ''

httpRoute:
  enabled: true
  parentRefs:
    - name: shared-gateway
      namespace: istio-system
  hostnames:
    - ''

persistence:
  config:
    annotations:
      argocd.argoproj.io/sync-options: Prune=false
    storageClass: longhorn
  media:
    accessMode: ReadWriteMany
    annotations:
      argocd.argoproj.io/sync-options: Prune=false
    size: 100Gi
    storageClass: longhorn

metrics:
  enabled: true

# Intel UHD 600 (J4105) VAAPI hardware transcoding
podSecurityContext:
  supplementalGroups:
    - 44   # video group (/dev/dri/card0)
    - 993  # render group (/dev/dri/renderD128)

volumes:
  - name: dri
    hostPath:
      path: /dev/dri
      type: Directory

volumeMounts:
  - name: dri
    mountPath: /dev/dri

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 4000m
    memory: 4Gi
